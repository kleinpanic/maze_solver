{
  "name": "maze-solver-web",
  "version": "0.2.1",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "tailwindcss -i ./src/styles.css -o ./dist/styles.css --minify && cp index.html dist/index.html && cp src/main.js dist/main.js && cp favicon.svg dist/favicon.svg",
    "dev": "tailwindcss -i ./src/styles.css -o ./dist/styles.css --watch",
    "test": "node --check src/main.js"
  },
  "devDependencies": {
    "@tailwindcss/cli": "^4.1.0",
    "playwright": "^1.60.0",
    "tailwindcss": "^4.1.0"
  }
}
